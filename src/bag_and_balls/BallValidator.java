package bag_and_balls;

import java.util.List;

class BallValidator {
//    private final int MAX_BALL_LIMIT = 12;
//    private final int MAX_GREEN_BALL_LIMIT = 3;
////    private final List<String> rules;
//
//    boolean canBeAdded(Ball ball, List<Ball> balls) {
//
////        for (String rule : rules) {
////            if(!rule.check(ball,balls)){
////                return false;
////            }
////        }
////        return true;
//
//
////        if (balls.size() >= MAX_BALL_LIMIT) return false;
////
////        if (ball.equals(Ball.GREEN)) {
////            return getBallCountInList(ball, balls) < MAX_GREEN_BALL_LIMIT;
////        }
////
////        if (ball.equals(Ball.RED)) {
////            int noOfGreenBalls = getBallCountInList(Ball.GREEN, balls);
////            int noOfRedBalls = getBallCountInList(Ball.RED, balls);
////            return false;
////        }
////
////        return true;
//    }
//
//    private int getBallCountInList(Ball ball, List<Ball> balls) {
//        int ballCount = 0;
//        for (Ball currentBall : balls) {
//            if (ball.equals(currentBall)) ballCount += 1;
//        }
//        return ballCount;
//    }
}
